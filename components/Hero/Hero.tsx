import { Box } from 'theme-ui'
import { HeroHeading } from './Heading'

export const Hero: React.FC = () => (
  <Box as="section" mt={[4, 4, 5, 5, 6]} mb={[4, 5, 5, 5, 6]}>
    <HeroHeading>Ik bouw ervaringen die bij mensen blijven hangen</HeroHeading>
  </Box>
)
